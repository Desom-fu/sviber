# sviber 0.3.6

## 本次完成内容

### 片段面板

- 修正片段行的 CSS 优先级覆盖问题。片段现在使用独立的 `42px` 缩略图列、名称列和五个操作按钮列，不会再因为通用吸附器行样式覆盖而出现缩略图压住名称、按钮溢出或名称压住按钮。
- 片段缩略图继续支持附着事件、嵌套 group 和按内容自动取景。
- 增加浏览器几何回归，检查缩略图尺寸、名称与按钮之间的边界以及操作按钮数量。

### 快捷键窗口

- 按 Vimium 风格将快捷键按文件、编辑、事件、吸附器、变换、音乐、帮助等功能分组。
- 宽窗口默认显示两列独立内容；每行左侧是带灰色小方框的快捷键，右侧是命令名称，分组标题居中显示。
- 每个分组根据最长快捷键自适应第一列宽度，分组内所有命令说明从同一竖线开始；中英文组合键都保持一致对齐。
- 可用宽度不足时自动切换为一列；窗口主体只允许纵向滚动，宽屏和窄屏都不会出现左右滚动条。
- 每一行仍注册命令 tooltip，鼠标悬停时提示栏显示完整命令说明。
- 对话框支持专用宽度样式，不影响其他表单对话框。

### 文案和帮助文档

- 将中英文界面与手册中的 `Bezier` 统一修正为带重音的 `Bézier`，中文显示为 `Bézier 曲线`。
- README、简体中文 README 和 HTML 手册补充了快捷键分组、双列/窄屏响应式布局、悬停提示和片段布局说明。
- 保留帮助文档全文搜索、自动定位和按文档顺序循环查找功能。

### 拖动性能

- 主编辑区、时间轴和自由变换预览继续使用 RAF 合并 pointermove；事件位置、角度、Tip point 和吸附器手柄的绝对拖动采用增量预览，时间轴事件移动按相邻输入的拍位/通道增量应用。
- 拖动预览不再每次重建 Inspector、片段、通道、历史和命令状态等完整面板；只更新渲染画布和状态栏，结束拖动时再通过一次完整刷新、历史记录和脏状态提交。
- 时间轴的增量拖动仍在结束时从原始快照重放总位移，保留 Ctrl 复制、通道边界、撤销和取消预览语义。
- 新增真实主编辑区/时间轴指针拖动回归，采集 RAF 间隔并检查交互期间的 60 Hz 帧节奏。

## 验证

- `npm test`：158/158 通过，源码大小检查通过。
- `npm run verify:browser`：宽屏快捷键窗口双列无横向溢出；700px 窄屏自动单列且无横向溢出；片段布局截图和快捷键布局截图已写入 `test-results`；100000 事件播放/编辑基准无掉帧；真实主编辑区拖动 p95 为 16.8ms、0 掉帧，时间轴拖动 p95 为 16.9ms、1 个长帧；其余中英文、主题、交互和离线回归通过。
- `npm run build`：使用独立的 `D:\sunniesnow\sviber-build-0.3.6` 输出目录完成本地桌面构建，并验证源码目录的 `icon.ico` 与 `icon.png` 由构建生成且被 Git 忽略。
- `git diff --check`：补丁格式检查通过。

## 发布提交

```text
fix: release sviber 0.3.6 with clips and shortcut layout fixes
```
