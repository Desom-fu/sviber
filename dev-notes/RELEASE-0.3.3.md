# sviber 0.3.3

## 本次修复

### 分组自由变换

- 修复选中 `group` 后自由变换目标不完整的问题。
- 选中 group 时现在会递归收集 group 锚点、直接子事件、嵌套 group 的全部后代，以及后代事件引用的附着吸附器。
- 自由变换会同步作用于 group 锚点和所有直接/间接子事件；如果子事件附着到吸附器，也会把对应吸附器纳入变换边界并更新其变换矩阵。
- 保留附着事件的 `snapPoint` 引用，避免变换后错误地把事件拆成普通坐标。
- 增加回归测试，覆盖选中 group、附着子事件、吸附器变换、group 锚点移动和普通子事件移动。

### 帮助文档搜索

- 用户手册页头新增搜索框、清除按钮和实时匹配计数。
- 搜索按当前语言过滤标题、段落、列表项和表格行；目录会同步隐藏没有匹配内容的章节。
- 支持英文和简体中文提示、无结果状态，以及切换语言后继续搜索。
- 手册在窄窗口下会自动调整为两行工具栏布局。

### 其他发布内容

- 历史记录新增分组、解组、定时和片段操作的中英文 i18n 文案，避免显示原始键名。
- 版本号更新为 `0.3.3`，应用资源查询参数更新为 `app.js?v=28`。
- Service Worker 缓存版本更新为 `sviber-v39`，确保新编辑器和帮助文档不会被旧缓存覆盖。
- README、中文 README 和手册同步说明 group 自由变换行为。

## 验证

- `npm test`：152 项全部通过。
- `npm run verify:browser`：通过中文/英文界面、明暗主题、画布像素、离线重载、100000 事件性能、宏和工程流程检查。
- 浏览器交互回归：英文搜索 `free transform`、中文搜索 `自由变换`、清除搜索并恢复全文均通过。
- 浏览器画布回归：选中 group 后出现四个自由变换角点，拖动角点会改变 group 子事件位置，按 Enter 完成变换。

## 发布提交

建议提交信息：

```text
fix: release sviber 0.3.3 with group transforms and manual search
```
